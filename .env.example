# The mock mode is the safe local default. It requires no credentials.
RIGHTSRADAR_MODE=mock

# In hybrid mode, set each integration to mock or real independently.
# RIGHTSRADAR_MODE=hybrid
# RIGHTSRADAR_GEMINI_MODE=real
# RIGHTSRADAR_PARALLEL_MODE=real
# RIGHTSRADAR_REPOSITORY_MODE=real

# Cloud mode enables real Gemini on Vertex AI, Parallel Search/Extract, Firestore, and Cloud Storage.
# RIGHTSRADAR_MODE=cloud
# RIGHTSRADAR_GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
RIGHTSRADAR_GOOGLE_CLOUD_LOCATION=global
RIGHTSRADAR_GEMINI_MODEL=gemini-3.7-flash
# RIGHTSRADAR_PARALLEL_API_KEY=set-in-your-local-environment
# Bounds concurrent Search -> Extract -> Gemini lead pipelines within one case analysis.
RIGHTSRADAR_PARALLEL_MAX_CONCURRENCY=4
RIGHTSRADAR_FIRESTORE_COLLECTION=rightsrader_cases
# RIGHTSRADAR_CLOUD_STORAGE_BUCKET=your-cloud-storage-bucket

# Repository-only hybrid smoke: keep Gemini and Parallel mock while using Firestore and Cloud
# Storage. `make smoke-real` reads and writes a short disposable case and text asset, then attempts
# to remove the asset before the case and reports cleanup failures. It requires local Application
# Default Credentials (ADC).
# RIGHTSRADAR_MODE=hybrid
# RIGHTSRADAR_REPOSITORY_MODE=real
# RIGHTSRADAR_GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
# RIGHTSRADAR_FIRESTORE_COLLECTION=rightsrader_cases
# RIGHTSRADAR_CLOUD_STORAGE_BUCKET=your-private-bucket-name
# RIGHTSRADAR_GEMINI_MODE=mock
# RIGHTSRADAR_PARALLEL_MODE=mock

# A repository smoke run remains disabled unless this opt-in is true and repositories are real.
RIGHTSRADAR_ENABLE_REAL_SMOKE=false

# Private asset lifecycle reconciliation is an explicit real-repository operation. It stays
# disabled by default and never runs from the application request path.
RIGHTSRADAR_ENABLE_RECONCILIATION=false

# Exposed to the browser; do not put secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Comma-separated browser origins allowed to call the API. Defaults to localhost dev ports.
# RIGHTSRADAR_ALLOWED_ORIGINS=https://hackathon-project-web-five.vercel.app
# Maximum new analyses per UTC day in cloud mode; pre-analyzed cases stay readable.
RIGHTSRADAR_DAILY_ANALYSIS_CAP=25
# Leads with Intake confidence below this run through the ADK Clearance Adjudicator.
RIGHTSRADAR_ADJUDICATE_BELOW_CONFIDENCE=0.75

# Terraform foundation

This directory intentionally declares only the Google provider constraints and future deployment
variables. It creates no resources. Do not run `terraform apply` as part of local development.
